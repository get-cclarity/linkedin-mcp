{
    "name": "linkedin-mcp",
    "private": true,
    "description": "CClarity LinkedIn MCP connector — public packages workspace",
    "license": "MIT",
    "workspaces": [
        "packages/mcp",
        "packages/cli"
    ],
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "build": "npm run build --workspace=packages/mcp && npm run build --workspace=packages/cli",
        "test": "npm run test --workspace=packages/mcp",
        "clean": "npm run clean --workspace=packages/mcp && npm run clean --workspace=packages/cli"
    }
}
